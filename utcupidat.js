// Assuming channel and headerType are defined somewhere in your code
let channel = "example";
let headerType = "header";

let role = "".concat(channel, "-").concat(headerType);
console.log(role); // This will output the concatenated string
